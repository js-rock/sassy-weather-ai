Sassy Weather - Learning Log - After refactor

20260320 - "Milestone: changed models from gemma3 to gpt-oss:20b for more dynamic feedback and utilising thought processes. Swapped voice engine to edge-tss and battled with a bug that used a baby voice when user input returned 'None'."

20260323 - "Tried to change LLM to be conversational, needed to make big changes to llm_brain.py and main.py. Dealt with lots of looping errors because of it. Learnt what 'continue' code does and why it was giving me a headache.

	Today's outstanding notes: 
	#1.Follow up context loops back to the original prompt when typing gibberish
	#2.Need to redefine prompts as AI took instructions too literally, i.e. reads both 24hour time and 12 hour time together.
	#3.Sometimes it titles 'roast' before doing the 'roast user about chosen city' prompt.


20260324 - Battled all afternoon at returning AI voice logic when user types in gibberish. It's a coding / skills issue on my behalf. Finally got it sorted with Gemini's help and clean refactoring of code, but still needs fine adjustment.

	Today's outstanding notes: 
	#1.Follow up context loops back to the original prompt when typing gibberish <-------------- mostly fixed, just returns a roast when following up with a legit question.
	#2.Need to redefine prompts as AI took instructions too literally, i.e. reads both 24hour time and 12 hour time together. <---- not done yet
	#3.Sometimes it titles 'roast' before doing the 'roast user about chosen city' prompt. <---- not done yet
	#4.Get voice working but need a mic first. <---- not done yet. Testing the G933 headset now.

git commit -m "WIP: changed audio engine to playsound, fixed filelock issues, battled looping logic and user gibberish inputs. Need to sort context bug for follow ups."


20260325 - Spent the entire learning time block on fixing the roast to do a proper return when a follow-up question is asked. It worked out at the end, and i fell victim to AI hallucination when i believed it when i asked results for 'Manila next Tuesday' over the current city, Sydney. This is because weather_api is currently only coded for today's results, and the LLM (gpt-oss) likely made up the weather based on assumptions. Doing checks on Accuweather, timeanddate and google weather showed it wasn't too far off either.

	Today's outstanding notes: 
	#1. Change weather_api to 5 day / 3 hour
	#2. Test mic / voice commands

git commit -m "Fixed city memory logic and doing roast returns for legitimate follow-up questions. Cleaned up llm_brain.py spaghetti."


20260326 - Today was spent changing the LLM logic to return forecast weather, 5 days maximum as per OpenWweather API limit. But the LLM hallucinates with 40 points of data to 	return and returns jank. I verified it with cross checking OW's website. 
	Tried prompt engineering, changing codes, nothing was fixing the hallucinations. Only thing i didn't try was for the LLM to return the maximum temp instead but that means 	it still dealing with a lot of data causing its confusion.

	No git commit today.


20260327 - Big day trying to solve logic and failures of future forecasts along with the complexities of adding different cities during secondary prompting. 
	Settled for using Openweather's max temp in their API 3 hour forecasts, which causes a 2-3°C variance between raw API snapshots and "Official" web reports. 
	Cross-verified with AccuWeather and determined the variance is a result of model smoothing vs. raw data points. Prioritized data depth (5-day forecast) over real-time synchronization for LLM personality grounding.

	Outstanding tasks:
	#1. Test mic / voice commands
	#2. Revisit other personalities as they've been neglected to focus on 'sassy'
	#3. "Tomorrow" follow up prompt currently a band aid

git commit -m "git commit -m "Milestone: Transitioned from single-day snapshots to 5-day forecast logic with multi-city context sync."


20260329 - Unscheduled working on sassy! Had it in my brain that since i'm focused on privacy, does this app have any issues with it? 
	It turns out user prompts is the most vulnerbale as that's where code can be input. Of course! I added a 'sanitizer' project to limit user input and ended up reworking a chunk of main.py.
	Another big feature solved was the accuracy of the data. Refactored the code to get_daily_maxes to scan for the daily high amidst the confusion of OW's 5day/3hr forecast API.
	Key Features Working:
		Context-aware city detection ("I'll be in Montreal on Wednesday")
		Proper date handling (tomorrow, specific days)
		12-hour time format for sunset display on print and adjusted prompt

	Outstanding tasks:
	# 1.Test mic / voice commands
	# 2.Revisit other personalities as they've been neglected to focus on 'sassy'
	# 3.Distill app down to an android version
	# 5.Github push?


20260330 - Optimised and hardened Sassy persona today adjusting  prompts just not for data accuracy, but to handle user typos (sanitized of course!). 
	Added precipation to the stats along with wind chill factor that changes between northern/souther hemispehere,
	by adding to the get_daily_maxes that was figured out yesterday. 

	Oustanding tasks:
	# 1.GitHub Push 
	# 2.Revisit other personalities as they've been neglected to focus on 'sassy'
	# 3.Local Web UI
	# 4.Mic/Voice Test
	# 5.The Android App
		
git commit --amend -m "Optimized Sassy persona for parsing additional weather data with roasts and location sanitizing and dealing with typos."
